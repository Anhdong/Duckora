---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
# image will be published to ghcr.io/anhdong/duckora

name: duckora
description: Mobun's opinionated Fedora Silverblue setup. Duck-approved!

base-image: ghcr.io/blue-build/base-images/fedora-silverblue
image-version: 44


modules:
  - type: os-release
    properties:
      NAME: Duckora
      PRETTY_NAME: Duckora(Silverblue)

  - type: files
    files:
      - source: system
        destination: /

  - type: dnf
    repos:
      cleanup: true
      files:
        - vscodium.repo
      copr:
        - atim/starship
    install:
      skip-unavailable: true
      install-weak-deps: false
      allow-erasing: true
      packages:
        - bat
        - eza
        - zoxide
        - starship
        - stow
        - codium
        - nvim
    remove:
      auto-remove: true
      packages:
        - gnome-system-monitor
        - gnome-software
        - firefox
        - firefox-langpacks
        - htop
        - nvtop

  - type: script
    scripts:
    - install_ble.sh

  - type: brew
    nofile-limits: true
    brew-analytics: false

  - type: default-flatpaks
    configurations:
      - scope: system
        repo:
          title: Flathub (system)
        install:
          - io.github.kolunmi.Bazaar
          - com.github.tchx84.Flatseal
          - io.github.flattool.Warehouse
          - com.ranfdev.DistroShelf
          - io.missioncenter.MissionCenter
          - com.mattjakeman.ExtensionManager
          - org.gnome.Loupe
          - org.gnome.Calendar
          - com.vixalien.sticky
          - io.gitlab.adhami3310.Impression
          - org.localsend.localsend_app
          - app.drey.Warp
          - app.zen_browser.zen
          - com.protonvpn.www

      - scope: user
        repo:
          title: Flathub (user)
        notify: false
        install:
          - org.onlyoffice.desktopeditors
          - com.notesnook.Notesnook
          - dev.vencord.Vesktop
          - io.github.Faugus.faugus-launcher
          - io.ente.auth
          - com.bitwarden.desktop
          - org.kde.kdenlive
          - org.inkscape.Inkscape
          - org.audacityteam.Audacity
          - com.obsproject.Studio
          - org.godotengine.Godot

  - type: gnome-extensions
    install:
      - Blur my Shell
      - Dash2Dock Animated
      - Just Perfection
      - Logo Menu
      - 779 #Clipboard Indicator
      - Color Picker
      - GNOME Fuzzy App Search
      - GSConnect
      - Vitals
      # Search light (not availble in 44 yet)
    uninstall:
      - Apps Menu
      - Background Logo
      - Launch New Instance
      # Window List (have duplicate cannot solve by ID)
      - Places Status Indicator

  - type: initramfs #Rebuild the boot ram disk so it could update plymouth theme

  - type: signing # this sets up the proper policy & signing files for signed images to work fully
